Buildear usando la página

En la misma carpeta


docker run --rm -v "$PWD":/usr/src/myapp -w /usr/src/myapp gcc:4.9 gcc -o code code.c




En PHP

docker run --rm -v "$PWD":/usr/src/myapp -w /usr/src/myapp gcc:4.9 gcc -o main code.c

./main <in >out.txt

 cat out.txt
